@layout() 
  <div class="card w-96 bg-primary text-primary-content">
    <div class="card-body">
        <h2 class="card-title">Message for {{auth.user.username}}</h2>
        <p>{{message.message}}</p>
      
    </div>
</div>
@end